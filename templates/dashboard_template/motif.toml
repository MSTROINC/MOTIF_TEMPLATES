# This is a config for E2B sandbox template.
# You can use template ID or template name (mspace-react-dashboard) to create a sandbox:

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("mspace-react-dashboard") # Sync sandbox
# sandbox = await AsyncSandbox.create("mspace-react-dashboard") # Async sandbox

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('mspace-react-dashboard')

team_id = "4f145bbd-574d-49c8-b091-3241ea2a6bd4"
start_cmd = "/compile_page.sh"
dockerfile = "motif.Dockerfile"
motif_name = "mspace-react-dashboard"
# motif_id = "" # Assigned by E2B after first publish — save it here
